{# templates/_components/contact/icon.html.twig #}
{% macro render(name) %}
    <div class="w-12 h-12 rounded-full bg-blue-50 dark:bg-blue-200 flex items-center justify-center text-primary dark:text-blue-400 shrink-0">
        <span class="material-symbols-outlined">{{ name }}</span>
    </div>
{% endmacro %}
