{# templates/_components/contact/icon.html.twig #}
{% macro render(name) %}
    <div class="w-10 h-10 rounded-full bg-blue-50 dark:bg-slate-50 flex items-center justify-center text-primary dark:text-primary-light shrink-0">
        <span class="material-symbols-outlined">{{ name }}</span>
    </div>
{% endmacro %}
