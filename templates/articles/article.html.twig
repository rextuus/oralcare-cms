{% extends 'base.html.twig' %}

{% block content %}
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16 sm:py-24">
        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-display font-bold text-slate-900 dark:text-white mb-8">{{ content.title }}</h1>

        <div class="prose prose-lg prose-slate dark:prose-invert max-w-none">
            {{ content.article|raw }}
        </div>
    </div>
{% endblock %}
