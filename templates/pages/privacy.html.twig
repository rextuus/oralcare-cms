{% extends 'base.html.twig' %}

{% block content %}
    <div class="pt-24 sm:pt-32 pb-20 px-4 sm:px-6 lg:px-8 max-w-4xl mx-auto">
        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-display font-bold mb-12 text-slate-900 dark:text-white">{{ content.title }}</h1>

        <div class="prose prose-lg dark:prose-invert max-w-none">
            {{ content.article|raw }}
        </div>
    </div>
{% endblock %}
