fos_rest:
    zone:
        - { path: ^/admin/* }
    format_listener:
        rules:
            - { path: ^/admin/api, priorities: [json], fallback_format: json, prefer_extension: true }
            - { path: ^/, priorities: [html, '*/*'], fallback_format: html, prefer_extension: true }
